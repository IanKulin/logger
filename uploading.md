Since I do this so infrequently:

- At the command line `npm login` (if you haven't used this machine before)
- You get a browser link to NPM and will need the 2FA app you set it up with
- `npm publish --access public`
- Once again, 2FA
- The upload confirmation is the version number